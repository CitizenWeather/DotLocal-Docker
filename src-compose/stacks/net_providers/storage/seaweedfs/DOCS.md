# seaweedfs storage slot

Slot path: `stacks/net_providers/storage/seaweedfs/`

To activate: set `STORAGE_APP=seaweedfs` in `.env`.

Image: `chrislusf/seaweedfs:latest`
S3 API compatible; runs master + volume + filer + S3 gateway.
